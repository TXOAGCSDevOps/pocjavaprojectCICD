name: Build Java Application - Development

on:
  repository_dispatch:
    types: [dev]
  push:
    branches:
    - master

jobs:
  build:
    runs-on: oag_runner

    steps:
      - name: Checkout Java Code
        uses: actions/checkout@v2
        with:
          repository: atdevsecops/pocjavaproject
          ref: develop
          token: ${{ secrets.SECRET_TOKEN }}

      - name: Set up JDK 17
        uses: actions/setup-java@v4.2.1
        with:
          java-version: '17'
          distribution: 'zulu'

      - name: Install Maven
        uses: stCarolas/setup-maven@v5
        with:
          maven-version: 3.9.2

      - name: Build with Maven
        run: mvn -B package

      - name: Set up JFrog CLI
        uses: jfrog/setup-jfrog-cli@v4.1.3
        with:
          version: '2.61.0'

      - name: Configure JFrog CLI
        env:
          JFROG_URL: ${{ env.JFROG_URL }}
          JFROG_USER: ${{ secrets.JFROG_USER }}
          JFROG_PASSWORD: ${{ secrets.JFROG_PASSWORD }}
        run: |
          jfrog config add dev-config --url=http://jfrog-platform-artifactory.artifactory.svc.cluster.local:8082/artifactory --user=$JFROG_USER --password=$JFROG_PASSWORD

      - name: Upload to Artifactory
        run: jfrog rt u "target/*.jar" "APPS/Java/artifact/"
